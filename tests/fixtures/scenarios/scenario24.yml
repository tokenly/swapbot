# simple live transaction received
#   tests that payment account is debited (txfee0002)

############################
# setup bots and payment
bots:
  - meta: { baseFilename: bot01.yml }
    payment_plan: txfee002


############################
# receive notifications from xchain
xchainNotifications:
  - meta: { baseFilename: confirmed_LTBCOIN_RECEIVE_notification_01.yml }


############################
# validate the expected results
expectedXChainCalls:
  - 
    meta: { baseFilename: send_BTC.yml }

expectedBotEvents:
  - #### NEW
    meta: { baseFilename: swap.new.yml }
  - #### READY
    meta: { baseFilename: swap.stateChange-ready.yml }
  - #### swap found
    meta: { baseFilename: swap.found.yml }
  - #### swap sent
    meta: { baseFilename: swap.sent.yml }
  - #### SENT
    meta: { baseFilename: swap.stateChange-sent.yml }
  - #### transaction fee paid
    meta: { baseFilename: fee.transaction.yml }
    msg: Paid a transaction fee of 0.0005.
    fee: 0.0005

expectedBotLedgerEntries:
  - meta: { baseFilename: bot_ledger_entry_model_TEST_SETUP.yml }
  - 
    meta: { baseFilename: bot_ledger_entry_model_TX_DEBIT.yml }
    amount: 50000 # 0.0005

